# Production release checkpoint

## Ready locally

- Typed natural-language query plans and deterministic SQL compilation.
- Optional OpenAI Responses API planner with rules fallback.
- Bounded AI routes and a visible evidence-backed dashboard experience.
- Vercel root entrypoint and pooled managed-database configuration.
- Manual official-data load workflow, reproducible dependency lock and hardened CI.
- Recruiter-first README, MIT license, dashboard screenshot and social preview.

## Requires Sofia's external authorization/accounts

- Create or select the public GitHub repository and push `main`.
- Create Neon roles/database and save writer/runtime secrets.
- Run the data workflow against a disposable branch, then production.
- Import the repository into Vercel and configure environment variables.
- Verify public URLs, update README links, publish `v1.0.0` and pin the repository.

No cloud resource, public repository, paid API call or external secret was created during the local
implementation.
